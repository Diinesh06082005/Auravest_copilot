# AuraVest Copilot ✦ Frontend Component & Theme Documentation

This directory contains the frontend user interface, layout views, charts, and state stores.

---

# 🎨 Frontend Component Implementation Details

Detailed implementation details of the DynamicIsland UI Component:

The DynamicIsland component is written in React 19 using TypeScript for strict
typing. It subscribes to specific Zustand stores and implements custom CSS
classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion to
handle entry animations and scale transitions.

---

Detailed implementation details of the LoadingChecklist UI Component:

The LoadingChecklist component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the LoadingGraphAnimation UI Component:

The LoadingGraphAnimation component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the NewsTickerFeed UI Component:

The NewsTickerFeed component is written in React 19 using TypeScript for strict
typing. It subscribes to specific Zustand stores and implements custom CSS
classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion to
handle entry animations and scale transitions.

---

Detailed implementation details of the HistoricalSVGChart UI Component:

The HistoricalSVGChart component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the FactorRadarChart UI Component:

The FactorRadarChart component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the AssetAllocatorSlider UI Component:

The AssetAllocatorSlider component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the YouTubeWidgetPanel UI Component:

The YouTubeWidgetPanel component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the CommandPaletteModal UI Component:

The CommandPaletteModal component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the NavbarHeader UI Component:

The NavbarHeader component is written in React 19 using TypeScript for strict
typing. It subscribes to specific Zustand stores and implements custom CSS
classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion to
handle entry animations and scale transitions.

---

Detailed implementation details of the SidebarNavigation UI Component:

The SidebarNavigation component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

Detailed implementation details of the AuthLoginPageCard UI Component:

The AuthLoginPageCard component is written in React 19 using TypeScript for
strict typing. It subscribes to specific Zustand stores and implements custom
CSS classes supporting Light, Dark, and Glass mode styles. It uses Framer Motion
to handle entry animations and scale transitions.

---

---

# 💅 CSS Styling Classes and Theme Tokens

CSS Design Token 1: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 2: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 3: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 4: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 5: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 6: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 7: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 8: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 9: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 10: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 11: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 12: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 13: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 14: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 15: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 16: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 17: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 18: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 19: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 20: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 21: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 22: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 23: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 24: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 25: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 26: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 27: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 28: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 29: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 30: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 31: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 32: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 33: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 34: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 35: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 36: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 37: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 38: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 39: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 40: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 41: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 42: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 43: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 44: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 45: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 46: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 47: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 48: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 49: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 50: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 51: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 52: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 53: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 54: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 55: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 56: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 57: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 58: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 59: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 60: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 61: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 62: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 63: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 64: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 65: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 66: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 67: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 68: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 69: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 70: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 71: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 72: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 73: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 74: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 75: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 76: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 77: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 78: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 79: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

CSS Design Token 80: Detailed explanation of layout, typography, or spacing
variable.

This token defines spacing, background opacity, or font sizing settings across
AuraVest. In Glass mode, this styling utilizes translucent color values (like
white with low opacity) coupled with blur filters to produce a premium look. In
Dark mode, it switches to low-contrast slate colors to reduce eye strain, while
Light mode relies on high-contrast colors. CSS transitions ensure smooth visual
state updates when switching modes.

---

# 📂 Frontend File Catalog & Implementation

Detailed analysis of frontend/src/presentation/pages/dashboard/index.tsx: The
main dashboard layout, managing loading states and scroll-to-top behaviors.

Detailed analysis of frontend/src/presentation/components/common/Navbar.tsx:
Renders the header, theme toggle, and search command palette.

Detailed analysis of frontend/src/presentation/components/common/Sidebar.tsx:
Houses side navigation routes and active page indicators.

Detailed analysis of frontend/src/presentation/components/dashboard/AIChat.tsx:
The chat widget. It uses inner container scrollTop adjustments to scroll without
displacing the main viewport.

Detailed analysis of
frontend/src/presentation/components/dashboard/Watchlist.tsx: Renders watchlists
and handles add/remove events.

Detailed analysis of
frontend/src/presentation/components/dashboard/RecommendationCard.tsx: Displays
consensus ratings (Buy/Hold/Sell) with custom theme colors.

Detailed analysis of
frontend/src/presentation/components/dashboard/DynamicIsland.tsx: Displays
real-time research progress percentages in a morphing pill.

Detailed analysis of
frontend/src/presentation/components/dashboard/LoadingGraphAnimation.tsx:
Renders an animated SVG wave chart during loading screens.

Detailed analysis of frontend/src/business/store/research.store.ts: The Zustand
store managing research state and SSE event parsing.

Detailed analysis of frontend/src/business/store/auth.store.ts: The Zustand
store managing login, registration, and user sessions.

---
